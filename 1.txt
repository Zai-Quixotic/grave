mkdir nama-folder
cd nama-folder
git init

buat file.txt di folder yg baru dibikin

git status

lakukan perubahan di file.txt

git status
git add file.txt
git commit -m "pesan commit"

buat repo di github.com/new

copy perintah terakhir yg ada di github repo:
  contoh: git remote add origin ....
          git branch -M main
          git push -u origin main